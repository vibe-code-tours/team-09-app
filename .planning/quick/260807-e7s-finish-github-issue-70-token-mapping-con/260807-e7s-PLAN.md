---
phase: 260807-e7s-finish-github-issue-70-token-mapping-con
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BackgroundTitleModal.tsx
  - src/components/CreateSheet.tsx
  - src/components/ElevatedTabBar.tsx
  - src/screens/DayDetailScreen.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/RecordScreen.tsx
  - src/screens/SearchScreen.tsx
  - src/screens/WeeklySummaryScreen.tsx
  - src/components/TimePickerModal.tsx
  - src/components/PinLimitModal.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Every spacing/borderRadius literal in the 7 editable files that maps exactly onto the token scale (4/8/12/16/20/24/32 → spacing, 8/12/16/20 → radius) references a token; no exact-token literals remain un-tokenized
    - TimePickerModal and PinLimitModal are verified to contain only fixed scroll/icon geometry (80/132/44/24) and no token-able literals
    - All 7 editable files import `spacing` and `radius` from `../theme`
    - TypeScript strict mode passes (`npx tsc --noEmit` exit 0)
    - All code changes are left UNCOMMITTED in the working tree for user review
  artifacts:
    - src/components/BackgroundTitleModal.tsx
    - src/components/CreateSheet.tsx
    - src/components/ElevatedTabBar.tsx
    - src/screens/DayDetailScreen.tsx
    - src/screens/HomeScreen.tsx
    - src/screens/RecordScreen.tsx
    - src/screens/WeeklySummaryScreen.tsx
  key_links:
    - BackgroundTitleModal/CreateSheet now import spacing+radius from ../theme (BackgroundTitleModal currently imports only createShadows)
    - RecordScreen statusContainer/actionContainer paddingHorizontal 40 now `spacing.xxxl + spacing.sm` compound, matching CreateSheet's sheet.paddingBottom treatment
---

<objective>
Finish GitHub issue #70 finding #1 (non-responsive hardcoded CSS → design tokens) by converting the remaining trivially-token-able spacing/borderRadius literals in BackgroundTitleModal, CreateSheet, ElevatedTabBar, and the leftover gap/margin values in DayDetailScreen, HomeScreen, RecordScreen, and WeeklySummaryScreen, plus verifying TimePickerModal, PinLimitModal, and SearchScreen are already clean.

Purpose: Complete token coverage so no exact-scale spacing/borderRadius literal remains in the app's style code — closing the final gap left by 260805-krn and 260806-gg3.

Output: 7 files with pure style-literal swaps (zero functional/layout change), 3 verify-only files confirmed clean, tsc clean.
</objective>

<execution_context>
@/Users/thetnainglin/ForMyImprovement/kokoye/team-09-app/.claude/gsd-core/workflows/execute-plan.md
@/Users/thetnainglin/ForMyImprovement/kokoye/team-09-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260805-krn-fix-github-issue-70-ui-ux-audit-findings/260805-krn-SUMMARY.md
@.planning/quick/260806-gg3-finish-github-issue-70-ui-ux-audit-findi/260806-gg3-SUMMARY.md
@src/theme/index.ts
@src/components/RecordScreen.tsx (already-converted modal styles in src/screens/RecordScreen.tsx — reference pattern for the modal conversions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize modal components BackgroundTitleModal + CreateSheet</name>
  <files>src/components/BackgroundTitleModal.tsx, src/components/CreateSheet.tsx</files>
  <action>
    Convert exact-token spacing/borderRadius literals in both files' StyleSheet styles (per the verified line inventory below). Pure style-literal swaps — no layout/functional change, no typography/geometry/color edits.

    **BackgroundTitleModal.tsx** (styles at lines 220-279): extend the existing `../theme` import on line 21 from `createShadows` to `spacing, radius, createShadows` (alphabetical order matching project convention). Convert:
    - modalOverlay.paddingHorizontal: 32 → spacing.xxxl
    - modalContent.borderRadius: 20 → radius.xl
    - modalTitle.marginBottom: 4 → spacing.xs
    - modalSubtitle.marginBottom: 20 → spacing.xl
    - modalInput.borderRadius: 12 → radius.md
    - modalInput.paddingHorizontal: 16 → spacing.lg
    - modalInput.marginBottom: 20 → spacing.xl
    - modalButtons.gap: 12 → spacing.md
    - modalSkipBtn.borderRadius: 12 → radius.md
    - modalSaveBtn.borderRadius: 12 → radius.md
    LEAVE: modalContent.padding: 28 (not on scale), modalInput.paddingVertical: 14, modalSkipBtn.paddingVertical: 14, modalSaveBtn.paddingVertical: 14 (14 not on scale), typography (fontSize 20/14/16), width: '100%', overlay rgba(0,0,0,0.5), borderWidth: 1. These mirrors the RecordScreen modal treatment exactly (its modalContent kept padding 28 and modalInput kept paddingVertical 14).

    **CreateSheet.tsx** (styles at lines 184-237): add `import { spacing, radius } from '../theme';` after the existing `useTheme` import (line 14) — it currently has NO theme-token import. Convert:
    - sheet.borderTopLeftRadius: 20 → radius.xl
    - sheet.borderTopRightRadius: 20 → radius.xl
    - sheet.paddingBottom: 40 → spacing.xxxl + spacing.sm (house-style compound, 32+8=40)
    - handle.marginTop: 12 → spacing.md
    - handle.marginBottom: 16 → spacing.lg
    - title.paddingHorizontal: 20 → spacing.xl
    - title.marginBottom: 16 → spacing.lg
    - option.paddingHorizontal: 20 → spacing.xl
    - option.paddingVertical: 16 → spacing.lg
    - optionIcon.borderRadius: 12 → radius.md
    - optionText.marginLeft: 12 → spacing.md
    - optionSubtitle.marginTop: 4 → spacing.xs
    LEAVE: sheet.maxHeight: SCREEN_HEIGHT * 0.5 (responsive percentage), handle width: 40 / height: 4 / borderRadius: 2 (fixed grab-handle geometry, 4/2 not on scale), optionIcon width/height: 48 (fixed touch-target icon circle), option width: 40 (non-existent in file — do not touch), typography (fontSize 18/16/14).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <grep>grep -n "spacing\.\|radius\." src/components/BackgroundTitleModal.tsx src/components/CreateSheet.tsx | wc -l (expect ≥ 22 token references across both files)</grep>
  </verify>
  <done>All 22 listed conversions applied; both files import spacing+radius from ../theme; npx tsc --noEmit exits 0; changes uncommitted</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize ElevatedTabBar + screen leftovers (DayDetail, Home, Record, WeeklySummary)</name>
  <files>src/components/ElevatedTabBar.tsx, src/screens/DayDetailScreen.tsx, src/screens/HomeScreen.tsx, src/screens/RecordScreen.tsx, src/screens/WeeklySummaryScreen.tsx</files>
  <action>
    Convert exact-token literals in five files. All already import spacing/radius from ../theme (verified — no import changes needed). Pure style-literal swaps only.

    **ElevatedTabBar.tsx** (styles at lines 100-143): `tabLabel.marginTop: 4` → spacing.xs; `centerLabel.marginTop: 4` → spacing.xs. LEAVE: centerButton borderRadius: 32 (half of the fixed 64px circle — same rationale as RecordScreen's controlBtn 32), width/height 64, borderWidth 4, typography (fontSize 11).

    **DayDetailScreen.tsx**: only `entryContent.gap: 4` → spacing.xs (line 400). LEAVE: headerSubtitle.marginTop: 2, categoryTag paddingHorizontal 6 / paddingVertical 2 / borderRadius 4 (not on token scale; small tag chip), entryIconBg 36 (fixed icon square), typography.

    **HomeScreen.tsx**: only `entryContent.gap: 4` → spacing.xs (line 617 area). LEAVE: headerAvatar.borderRadius: 20 (half of fixed 40px avatar circle), categoryTag 6/2/4, typography.

    **RecordScreen.tsx**: `statusContainer.paddingHorizontal: 40` (line 756) and `actionContainer.paddingHorizontal: 40` (line 782) → `spacing.xxxl + spacing.sm` (house-style compound 32+8=40 — matches CreateSheet's sheet.paddingBottom treatment in Task 1). LEAVE: discardButton.borderRadius: 25, modalContent.padding 28, headerSpacer 28, minHeight 140/64, controlBtn/stopBtn/playButton circle dims (already handled in prior passes).

    **WeeklySummaryScreen.tsx**: `statCard.gap: 4` (line 365) → spacing.xs; `categoryChip.gap: 4` (line 376) → spacing.xs; `moodItem.gap: 4` (line 388) → spacing.xs. LEAVE: categoryChip.paddingVertical: 6 (not on scale), statIcon/statValue/statLabel typography.

    Do NOT touch: SearchScreen (Task 3 verify-only — its only literals are searchInput.padding: 0 reset and clearBtn.padding: 2), NotesScreen (dayPreviewContent.gap: 2 not on scale), PinLimitModal, TimePickerModal.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <grep>grep -n "gap: 4\|paddingHorizontal: 40" src/screens/DayDetailScreen.tsx src/screens/HomeScreen.tsx src/screens/RecordScreen.tsx src/screens/WeeklySummaryScreen.tsx | grep -v spacing (expect 0 hits)</grep>
  </verify>
  <done>All 7 conversions applied across 5 files; grep gate shows no remaining `gap: 4` or `paddingHorizontal: 40` literals in the four screens; npx tsc --noEmit exits 0; changes uncommitted</done>
</task>

<task type="auto">
  <name>Task 3: Verify-only pass — TimePickerModal, PinLimitModal, SearchScreen</name>
  <files>src/components/TimePickerModal.tsx, src/components/PinLimitModal.tsx, src/screens/SearchScreen.tsx</files>
  <action>
    Verify these three files contain no token-able literals and make NO changes. Grep each file for numeric spacing/borderRadius patterns (paddingHorizontal/paddingVertical/marginTop/marginBottom/marginLeft/gap/borderRadius/width/height followed by bare numbers).

    **TimePickerModal.tsx**: expected remaining literals are ONLY fixed scroll geometry — scrollContainer.width: 80, scrollContainer.height: 132, pickerItem.height: 44 — plus typography. All are fixed scroll-window geometry (leave). Confirm no padding/margin/gap/borderRadius numeric literals remain. It already imports spacing/radius (line 14) — used where applicable.

    **PinLimitModal.tsx**: expected remaining literal is ONLY `width: 24` (line 119, fixed icon). Confirm no other padding/margin/gap/borderRadius numeric literals remain. It already imports spacing/radius (line 13).

    **SearchScreen.tsx**: expected remaining literals are ONLY searchInput.padding: 0 (reset value, not a spacing token) and clearBtn.padding: 2 (not on scale), plus typography. Confirm no other token-able literals remain. It already imports spacing/radius (line 18).

    If any additional token-able literal (exact-scale value) is found beyond these, convert it with the token. Otherwise make zero edits. Do NOT touch any of the listed leave-values.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <grep>grep -c "width: 80\|height: 132\|height: 44" src/components/TimePickerModal.tsx (expect 3); grep -c "width: 24" src/components/PinLimitModal.tsx (expect 1)</grep>
  </verify>
  <done>All three files confirmed to contain only fixed-geometry/reset literals and typography; no token-able literals remain; npx tsc --noEmit exits 0; no edits made (or only the discovered extra token-able literal converted); changes uncommitted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Style code → runtime | StyleSheet.create() values are computed at module load; a typo in a token reference produces a TypeScript error, not a runtime crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260807-01 | Spoofing | All edited files | low | mitigate | No user input, no network, no data access — style constants only; tokens are `as const` typed so tsc rejects invalid token names |
| T-260807-02 | Tampering | npm installs | low | accept | No new dependencies added (rules explicitly forbid new deps); no install commands run |
</threat_model>

<verification>
- `npx tsc --noEmit` from repo root after each task (strict mode, exit 0)
- Grep gates per task (token-reference counts, zero-remaining-literal checks)
- `git status` at end: exactly the 7 edited files modified + this plan directory; nothing committed
</verification>

<success_criteria>
- GitHub issue #70 finding #1 (hardcoded CSS → design tokens) is fully closed: every spacing/borderRadius literal that maps exactly onto the token scale now references spacing.* / radius.* across all edited components and screens
- Files with only non-token values (2/6/14/28, fixed circles/geometry, resets) documented and left untouched
- `npx tsc --noEmit` passes
- All code changes left UNCOMMITTED in the working tree — the user reviews, commits, and pushes themselves (per project memory note; do NOT run git commit for code)
</success_criteria>

<output>
Create `.planning/quick/260807-e7s-finish-github-issue-70-token-mapping-con/260807-e7s-SUMMARY.md` when done
</output>
