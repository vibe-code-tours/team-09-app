---
phase: 260806-gg3-finish-github-issue-70-ui-ux-audit-findi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/CreateNoteScreen.tsx
  - src/screens/SearchScreen.tsx
  - src/components/EmptyState.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/DayDetailScreen.tsx
  - src/screens/NotesScreen.tsx
autonomous: true
requirements: []
user_setup: []
---

<objective>
Finish resolving GitHub issue #70 UI/UX audit findings: (1) add skeleton loading states to the two remaining data-fetching screens (CreateNoteScreen, SearchScreen), and (2) complete the responsive layout pass — EmptyState illustration scales with screen width via `useWindowDimensions()`, swipe-action widths become readable token sums, and list scroll clearance becomes safe-area-aware.

Purpose: Quick task 260805-krn delivered the reusable `Skeleton` component and wired it into HomeScreen/NotesScreen/DayDetailScreen, plus token-mapped the hardcoded pixel styles. Two audit items remain: CreateNoteScreen renders blank fields until `getEntryById` resolves (a perceived-performance gap), SearchScreen shows a plain "Searching..." text instead of a skeleton; and the audit's "responsive layout" recommendation is only partially addressed — the EmptyState 120px illustration is fixed on all screen sizes, swipe-action widths are bare literals, and list bottom clearance is a raw 100px that ignores the gesture-nav safe-area inset.

Output: Skeleton loading states in CreateNoteScreen and SearchScreen, a window-responsive EmptyState illustration, token-sum swipe-action widths, and safe-area-aware scroll clearance — all type-checked, all behavior-preserving.

Note: The user reviews, commits, and pushes changes THEMSELVES. Leave ALL code changes uncommitted in the working tree. Do NOT run `git commit` or `git add` on code.
</objective>

<execution_context>
@/Users/thetnainglin/ForMyImprovement/kokoye/team-09-app/.claude/skills/sketch-findings-mhat-tan/SKILL.md
</execution_context>

<context>
Existing skeleton primitive (from prior quick task): `src/components/Skeleton.tsx` — props `{ width?: DimensionValue, height?: DimensionValue, borderRadius?: number (default radius.md), style?: StyleProp<ViewStyle> }`, renders a theme-aware (`colors.surfaceAlt`) pulsing `Animated.View`. Composites are composed by screens.

Established skeleton pattern (HomeScreen/NotesScreen/DayDetailScreen): `const [isLoading, setIsLoading] = useState(true)`; set `isLoading` true at the start of the load function; clear in `finally` guarded by the existing `cancelled` flag (`if (cancelled) return; setIsLoading(false)`); render `{isLoading ? <skeleton ScrollView scrollEnabled={false}> : <real content>}`. Skeleton cards mirror real card styles (`styles.entryCard`, `styles.entryMainRow`, `styles.entryContent`) so no layout shift. DayDetailScreen uses a right-side `styles.backButton` (width 40) as a header-balance spacer (line 314) — mirrors CreateNoteScreen's `styles.headerSpacer` usage in RecordScreen (line 423, width 28).

Design tokens: `src/theme/index.ts` — `spacing` {xs:4, sm:8, md:12, lg:16, xl:20, xxl:24, xxxl:32}, `radius` {sm:8, md:12, lg:16, xl:20, full:9999}. House style allows token-sum compounds (e.g. `spacing.xxl + spacing.sm`, `spacing.xxxl * 2`).

Scroll-clearance math (verified): `ElevatedTabBar` (`src/components/ElevatedTabBar.tsx`) is `position: 'absolute', bottom: 0` with 64px center button (marginTop -24 pulls it up) + tabRow `paddingTop: spacing.md` + label/tab paddings + `paddingBottom: insets.bottom`. Safe-area-aware replacement for `paddingBottom: 100` is `insets.bottom + spacing.xxxl * 3` (32*3 = 96 ≈ current 100 for zero-inset devices; grows on gesture-nav devices where the tab bar already adds `insets.bottom` to its own height). Pattern precedent: SearchScreen line 415 already uses `insets.bottom + 100`.

Files and exact locations to modify:
- `src/screens/CreateNoteScreen.tsx` — `loadEntry` (lines 108-125); import block (line 31 area); `header`/`headerActions`/`scrollContent`/`titleInput`/`toolsStrip`/`paperCard`/`audioSection`/`bottomActions` styles (lines 755+); `viewContentContainer` `maxHeight: 400` (line 864).
- `src/screens/SearchScreen.tsx` — `isSearching` state (line 117); debounced search effect (lines 183-208); `useFocusEffect` load (lines 140-161); `resultsContent` style (line 616); `emptyState` style (line 691).
- `src/components/EmptyState.tsx` — `useWindowDimensions` (line 17); illustration styles (lines 118-137); render (lines 58-76).
- `src/screens/HomeScreen.tsx` — `listContent.paddingBottom: 100` (line 737); `swipeAction.width: 80` (line 743).
- `src/screens/DayDetailScreen.tsx` — `listContent.paddingBottom: 100` (line 438); `swipeAction.width: 80` (line 453).
- `src/screens/NotesScreen.tsx` — `listContent.paddingBottom: 100` (line 388).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton loading state to CreateNoteScreen</name>
  <files>src/screens/CreateNoteScreen.tsx</files>
  <action>
    Add a skeleton loading state to CreateNoteScreen so opening an existing note (route param `entryId`) shows a placeholder mirroring the editor layout instead of blank fields while `getEntryById` resolves.

    1. Import: add `import { Skeleton } from '../components/Skeleton';` next to the existing `AudioPlayer` import (line 31 area). `spacing`/`radius` are already imported from `../theme` (line 21).

    2. State: add `const [isLoading, setIsLoading] = useState(false);` with the other useState declarations (line 75 area). Initialize `false` (new-note path needs no skeleton).

    3. `loadEntry` (lines 108-125): set `setIsLoading(true)` as the first statement (before the `try`). In the existing `catch`, keep the console.error as-is. Add a `finally { setIsLoading(false); }` block. Do NOT add a `cancelled` flag to `loadEntry` — it has none today and the effect at lines 80-94 has no cleanup; a `finally` without the guard is fine here and matches how `isDeletingAudio`/`isSaving` are cleared in this same file.

    4. Skeleton renderer (add as a component-scope function, e.g. after `handleTitleChange`, before the `return`):
       `const renderEditorSkeleton = () => (<View style={styles.scrollContent}>` containing:
       - Title placeholder: `<Skeleton width="55%" height={34} borderRadius={radius.sm} />` inside a View with `marginBottom: spacing.lg` (mirrors `titleInput` fontSize 28 + fontWeight 700; 34 approximates the two-line-input height).
       - Tools strip: `<View style={[styles.toolsStrip, { marginBottom: spacing.xl }]}>` with two pill skeletons `<Skeleton width={88} height={32} borderRadius={radius.full} />` separated by `gap: spacing.sm` (category pill and pin button — both real elements render in edit mode; do not render the strip inside the paperCard skeleton).
       - Audio placeholder (only when `initialAudioFile` is truthy — the real audio section only renders when `audioFile` exists): `<View style={styles.audioSection}>` containing `<Skeleton height={44} borderRadius={radius.md} style={{ flex: 1 }} />` (the AudioPlayer strip) plus a small right-side square `<Skeleton width={32} height={32} borderRadius={radius.sm} />` (delete-audio button placeholder). Wrap the audio block in `{initialAudioFile && ( ... )}`.
       - Paper card: `<View style={[styles.paperCard, { backgroundColor: colors.surfaceAlt, borderColor: colors.border }]}>` — mirror the REAL loaded card exactly (lines 602-610), with `minHeight: 200` coming from the style — containing three text lines: `<Skeleton width="92%" height={16} />`, `<Skeleton width="85%" height={16} />`, `<Skeleton width="60%" height={16} />` inside a View with `gap: spacing.md` (mirrors `contentInput` lineHeight 30).
       Close with `</View>`.
       Add a style `skeletonCardGap: { gap: spacing.md }` in the StyleSheet (or reuse `gap` inline — inline style arrays are theme-free here so either is acceptable; prefer the StyleSheet entry for the static `gap`).

    5. Render switch (lines 502-643): change the `KeyboardAvoidingView`'s child to:
       `{isLoading ? renderEditorSkeleton() : (<ScrollView style={styles.scrollView} contentContainerStyle={styles.scrollContent} keyboardDismissMode="interactive"> ...existing content unchanged... </ScrollView>)}`.
       The header (back button + actions), bottom actions bar, and PinLimitModal stay rendered and functional during load — the header shows the normal back button so the user can leave during load, and header actions (View/Edit/Save) are not interactive-meaningful on empty fields but keep the layout stable. Do NOT gate the header or bottom bar on isLoading.

    6. New-note path (else branch, lines 84-93) must NOT set isLoading — no skeleton for prefilled notes; verify nothing in that branch references the flag.

    Do NOT touch `viewContentContainer.maxHeight: 400` or any editor minHeight — these are intentionally tall editors per prior plan (Task 3 of 260805-krn left them as future-consideration). Do NOT alter loadEntry's behavior beyond adding the flag. Do NOT change any other file.
  </action>
  <verify>
    npx tsc --noEmit passes (strict mode; no new errors)
  </verify>
  <done>Opening an existing note (via HomeScreen or SearchScreen timeline navigation with entryId) shows skeleton placeholders (title line, tools-strip pills, audio strip when the note has audio, paper-card text lines) until getEntryById resolves, then the real editor renders without layout shift; new-note path renders the editor directly with no skeleton; `npx tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace SearchScreen "Searching..." text with skeleton result cards and add focus-load skeleton</name>
  <files>src/screens/SearchScreen.tsx</files>
  <action>
    Replace the centered "Searching..." text+icon in the results area with skeleton entry-card placeholders, and add a skeleton while the focus-effect load of `allEntries` runs.

    1. Import: add `import { Skeleton } from '../components/Skeleton';` to the import block (after line 23, the AudioPlayer import).

    2. Focus-load skeleton: add `const [isLoadingAll, setIsLoadingAll] = useState(true);` (line 117 area, next to `isSearching`). In the `useFocusEffect` callback (lines 140-161): the `loadData` function currently sets state inside `try` guarded by `cancelled`. Add `if (cancelled) return;` then `setIsLoadingAll(true);` as the first statements of `loadData` (before the `try`), and after the existing `try/catch` add `finally { if (!cancelled) setIsLoadingAll(false); }`. `isLoadingAll` starts `true` so the first focus shows a skeleton even before the effect's async load begins.

    3. Skeleton renderer (add near the bottom of the component, e.g. after the `displayEntries`/grouping derivations or just before `return`):
       `const renderSearchSkeleton = (cardCount: number) => (<View style={styles.resultsContent}>` — a plain View (NOT ScrollView; the outer results ScrollView already exists) containing `Array.from({ length: cardCount })` mapped to skeleton cards. Each card mirrors the REAL `styles.entryCard` (line 635: `borderRadius: radius.md, padding: spacing.md, marginVertical: spacing.xs, borderLeftWidth: 3` — the borderLeftColor is a category color, so use a neutral placeholder or omit the border color entirely; `borderLeftColor: 'transparent'` keeps the 3px width stable so the loaded card doesn't shift) with `{ backgroundColor: colors.surface }` and `shadows.sm` (both already in scope in this component). Card contents mirror `styles.entryMainRow` (`flexDirection: 'row', alignItems: 'center', gap: spacing.sm`): icon square `<Skeleton width={36} height={36} borderRadius={radius.sm} />`, then `<View style={styles.entryContent}>` (already `flex: 1, gap: 4`) with `<Skeleton width="70%" height={14} />` and `<Skeleton width={56} height={18} borderRadius={radius.sm} />`. Close with `</View>`, `</View>`.
       Do NOT add a separate StyleSheet entry for the card — reuse `styles.entryCard` with inline theme overrides (exactly the pattern HomeScreen/DayDetail use). If `styles.entryContent` has `gap: 4` which is not a token, leave it — it is a real style already in the file, not a new literal.

    4. Render switch (lines 419-424): replace the `isSearching ? (<View style={styles.emptyState}>...Searching...</View>)` branch with `isSearching ? renderSearchSkeleton(4) : ...`. Keep the `styles.emptyState` branch for the zero-results case (it is still used at line 424). The "Searching..." text inside the header result-count row (line 373) stays — it is a status label, not the results area.

    5. Browse skeleton: the ScrollView content (line 413-492) currently renders `isSearching ? ... : groupKeys.length === 0 ? ... : ...`. Chain `isLoadingAll` in front: `isLoadingAll ? renderSearchSkeleton(4) : isSearching ? renderSearchSkeleton(4) : groupKeys.length === 0 ? ... : ...`. Use 4 cards for both branches (matches HomeScreen's 4-card pattern). When `isLoadingAll` is true the query is necessarily empty (fresh focus), so no other branch can be active — the chain is deterministic.
       NOTE: during a browse (no query), `searchResults` is null and `displayEntries` derives from `filteredBrowseEntries` — when `isLoadingAll` is true, `allEntries` is still `[]`, so `groupKeys.length === 0` is true and the empty-state would flash before the skeleton change — the `isLoadingAll` branch fixes exactly that flash.

    6. `styles.resultsContent` (line 616) is `padding: spacing.xl` — reusing it for the skeleton keeps horizontal alignment with the real list; no style changes needed. Do NOT touch `styles.emptyState` or its `paddingTop: 80`.

    Do NOT change the debounce timing, the searchEntries call, the result-count row, or any other file.
  </action>
  <verify>
    npx tsc --noEmit passes (strict mode; no new errors)
  </verify>
  <done>While a debounced search runs, the results area shows 4 pulsing skeleton entry cards (icon square + title line + tag line) instead of the centered "Searching..." icon+text; on first focus, the same skeleton shows until allEntries/recentSearches load; results then replace the skeleton; the zero-results empty state still renders when a search completes with no matches; `npx tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Responsive layout pass — window-responsive EmptyState illustration, token-sum swipe widths, safe-area-aware scroll clearance</name>
  <files>src/components/EmptyState.tsx, src/screens/HomeScreen.tsx, src/screens/DayDetailScreen.tsx, src/screens/NotesScreen.tsx</files>
  <action>
    Complete the audit's responsive-layout recommendation with three changes. This is a pure style pass — zero functional/behavior changes.

    1. **EmptyState illustration scales with screen width** (`src/components/EmptyState.tsx`):
       - Add `useWindowDimensions` to the react-native import (line 3).
       - In the component: `const { width: screenWidth } = useWindowDimensions();` and `const illustrationSize = Math.min(120, screenWidth * 0.3);` — on a small screen (e.g. Mi 6, ~360dp wide: 108px) it shrinks slightly; on tablets it caps at the validated 120px sketch dimension (sketch 012-A validated the 120px illustration — never exceed it).
       - Replace the four hardcoded illustration dims with derived values: `styles.illustrationContainer` width/height 120 → `illustrationSize` (inline override, keep the static style entry for layout props like justifyContent/alignItems/marginBottom); `styles.pulseRing` width/height 120 → `illustrationSize` and `borderRadius: 60` → `illustrationSize / 2` (half of 120 — the ring must stay circular as it scales); `styles.iconCircle` width/height 96 → `illustrationSize * 0.8` and `borderRadius: 48` → `illustrationSize * 0.4`.
       - The `Ionicons mic size={48}` (line 74) stays fixed — icon font size is intentionally fixed per prior plan; the icon still fits the 108px minimum circle (96 * 0.9 = 86.4 > 48 + padding).
       - Do NOT change the pulse animation, the CTAs, `ctaRow.maxWidth: 320`, or `paddingVertical: spacing.xxxl * 2`.

    2. **Swipe-action widths become readable token sums** (`src/screens/HomeScreen.tsx` line 743, `src/screens/DayDetailScreen.tsx` line 453):
       - Replace `width: 80` with `width: spacing.xxl + spacing.xxxl * 2 - spacing.sm` — 24 + 64 - 8 = 80, an exact-token compound in the established house style (`spacing.xxl + spacing.sm`-type expressions). Same value, readable provenance, no layout change.
       - Do NOT change swipe icon sizes (fontSize 20), the 2px `marginBottom` on `swipeActionIcon`, or the `marginVertical: spacing.xs` — these are fine as-is.

    3. **List scroll clearance becomes safe-area-aware** (`src/screens/HomeScreen.tsx` line 737, `src/screens/DayDetailScreen.tsx` line 438, `src/screens/NotesScreen.tsx` line 388):
       - All three screens already import `SafeAreaView` from `react-native-safe-area-context` — extend each import to include `useSafeAreaInsets` (e.g. `import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';`).
       - In each component body add `const insets = useSafeAreaInsets();` (HomeScreen and DayDetailScreen already destructure several hooks; NotesScreen has a hook block too — place it with the other hooks).
       - Replace `paddingBottom: 100` in each `listContent` style with an inline override on the ScrollView/FlatList that uses `styles.listContent` (the same way the style is consumed today): add `paddingBottom: insets.bottom + spacing.xxxl * 3` to the inline style array of the ScrollView/FlatList that carries `contentContainerStyle={styles.listContent}` (32*3 = 96 ≈ the current 100 on zero-inset devices; on gesture-nav devices the clearance grows in lockstep with the tab bar's own `insets.bottom`, keeping the last card clear of the elevated FAB).
       - The skeleton ScrollViews in HomeScreen/DayDetailScreen/NotesScreen also use `styles.listContent` — since the change is an inline override on the real list's ScrollView/FlatList only, the skeleton wrappers keep the static 100 (they are scrollEnabled={false}, so clearance is cosmetic there). Do NOT touch the skeleton wrappers.
       - SearchScreen already uses `insets.bottom + 100` (line 415) — leave it; it is the established pattern this change aligns the other three screens with.

    Do NOT change: RecordScreen `headerSpacer` width 28, back-button width 40 touch targets, control-button widths 48/64/72, editor `minHeight` 200/300, hitSlop values, typography. These are intentionally fixed sizes (touch targets / tall editor UX) — documented as intentionally left. Do NOT touch App.tsx or ElevatedTabBar.
  </action>
  <verify>
    npx tsc --noEmit passes (strict mode; no new errors)
  </verify>
  <done>EmptyState illustration (container + pulse ring + icon circle) scales with `Math.min(120, screenWidth * 0.3)` and stays circular (radii = half of the derived size); swipe-action widths in HomeScreen and DayDetailScreen are token-sum expressions evaluating to exactly 80; HomeScreen, DayDetailScreen, and NotesScreen list scroll clearance is `insets.bottom + spacing.xxxl * 3` inline on the real list ScrollView/FlatList; `npx tsc --noEmit` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | UI-only change; no new data or network surface. Loading flags and skeleton rendering touch no user-controlled input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01 | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — no new dependencies; zero risk surface. |
| T-02 | Denial of Service | Skeleton pulse animation | low | accept | Animated.loop with native driver runs only while skeletons render; screens unmount them on load completion (existing pattern from prior quick task). Leftover animation would be cosmetic, not harmful. |
</threat_model>

<verification>
- `npx tsc --noEmit` from repo root passes after all three tasks (strict mode — the only executable check available; no test runner configured in package.json).
- Grep gate: `grep -n "import { Skeleton }" src/screens/CreateNoteScreen.tsx src/screens/SearchScreen.tsx` shows both imports; `grep -n "useWindowDimensions" src/components/EmptyState.tsx` shows the import and usage; `grep -n "insets.bottom + spacing.xxxl" src/screens/HomeScreen.tsx src/screens/DayDetailScreen.tsx src/screens/NotesScreen.tsx` shows three hits; `grep -n "spacing.xxl + spacing.xxxl \* 2 - spacing.sm" src/screens/HomeScreen.tsx src/screens/DayDetailScreen.tsx` shows two hits.
- Manual (user): on Android, open an existing note from the timeline — skeleton shows briefly then the editor populates; type a search — skeleton cards replace the old "Searching..." text; view the empty state on a small screen (e.g. Mi 6) — illustration scales down; verify last list items stay clear of the elevated center button with gesture navigation.
</verification>

<success_criteria>
- [ ] CreateNoteScreen shows a layout-mirroring skeleton while an existing note loads (edit-mode structure: title line, tools pills, audio strip when present, paper-card text lines); new-note path unaffected.
- [ ] SearchScreen shows 4 skeleton entry cards while searching and on first focus-load; zero-results empty state preserved.
- [ ] EmptyState illustration scales with `Math.min(120, screenWidth * 0.3)`; swipe widths are token sums (still 80); list clearance is `insets.bottom + spacing.xxxl * 3`.
- [ ] No functional behavior changed, no new dependencies, `npx tsc --noEmit` clean, all code changes left uncommitted for user review.
</success_criteria>

<output>
Create `.planning/quick/260806-gg3-finish-github-issue-70-ui-ux-audit-findi/260806-gg3-SUMMARY.md` when done. Do NOT git-commit code changes — the user reviews, commits, and pushes themselves.
</output>
