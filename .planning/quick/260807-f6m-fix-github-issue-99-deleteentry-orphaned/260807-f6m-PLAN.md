---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/storage.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - deleteEntry() removes the entry's audio file from disk before soft-deleting the DB record
    - Entries with no audioPath still soft-delete correctly (no crash)
  artifacts:
    - src/services/storage.ts (modified deleteEntry function)
  key_links:
    - storage.deleteEntry -> audioStorage.deleteAudioFile (new wiring)
---

<objective>
Fix GitHub issue #99: deleteEntry() orphaned audio files.

Purpose: When a user deletes an entry, the associated .m4a audio file is left on disk permanently. This wastes storage and creates orphaned files that are never cleaned up. The fix deletes the audio file before soft-deleting the DB record.

Output: Updated `deleteEntry()` in `src/services/storage.ts` that cleans up audio files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@src/services/storage.ts
@src/services/audioStorage.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix deleteEntry to clean up audio file before soft-delete</name>
  <files>src/services/storage.ts</files>
  <action>
Modify `deleteEntry` in `src/services/storage.ts` to remove the entry's audio file before soft-deleting the DB record.

**Step 1 — Add import:** Add `deleteAudioFile` to the existing import from `./audioStorage`. The current import on line 7 is `import { clearAllRecordings } from './audioStorage';`. Change it to `import { clearAllRecordings, deleteAudioFile } from './audioStorage';`.

**Step 2 — Rewrite `deleteEntry` function (currently lines 153-159):**

Before the soft-delete `db.update`, fetch the entry by ID to retrieve its `audioPath`:

1. Query: `const rows = await db.select({ audioPath: entries.audioPath }).from(entries).where(eq(entries.id, id)).limit(1);`
2. If the row exists and `rows[0].audioPath` is truthy, call `deleteAudioFile(rows[0].audioPath)` — the return value is a boolean but does not need to be checked; log a warning if false.
3. Then proceed with the existing soft-delete: `db.update(entries).set({ isDeleted: true, updatedAt: new Date() }).where(eq(entries.id, id));`

The function signature and return type (`Promise<void>`) remain unchanged. No new exports are needed.
  </action>
  <verify>
    <automated>grep -n 'deleteAudioFile' /Users/thetnainglin/ForMyImprovement/kokoye/team-09-app/src/services/storage.ts | grep -v '^.*:.*import' | grep -c 'deleteAudioFile' | xargs test 1 -le && grep -n 'audioPath' /Users/thetnainglin/ForMyImprovement/kokoye/team-09-app/src/services/storage.ts | grep -c 'select' | xargs test 1 -le && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `deleteAudioFile` is imported from `./audioStorage` in `storage.ts`
    - `deleteEntry` queries the entry for `audioPath` before soft-deleting
    - If `audioPath` exists, `deleteAudioFile(audioPath)` is called
    - The soft-delete still executes regardless of audio deletion outcome
    - TypeScript compiles without errors (`npx tsc --noEmit` passes)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `grep -c 'deleteAudioFile' src/services/storage.ts` returns at least 2 (import + call site)
- The `deleteEntry` function body contains both `audioPath` query and `deleteAudioFile` call
</verification>

<success_criteria>
- deleteEntry() no longer orphans audio files on disk
- Entries without audioPath (e.g. text-only notes) still soft-delete without error
- No regressions to other storage operations
</success_criteria>

<output>
Create `.planning/quick/260807-f6m-fix-github-issue-99-deleteentry-orphaned/260807-f6m-SUMMARY.md` when done
</output>
