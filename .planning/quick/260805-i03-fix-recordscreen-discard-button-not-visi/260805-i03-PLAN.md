---
phase: quick-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/RecordScreen.tsx
autonomous: true
requirements: []
---

<objective>
Fix RecordScreen bottom section overflow on small screens (Mi 6) so the Discard button is always reachable during and after transcription.

Purpose: On screens with limited height, the fixed top elements (header ~45px, timer ~108px, button container 160px) consume most of the viewport. The bottomSection (playback + status + discard button) overflows without scrolling, making the Discard button unreachable. Users can only discard via the header X button, which is not obvious.

Output: RecordScreen.tsx with a scrollable bottom section that adapts to screen size.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@src/screens/RecordScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make RecordScreen bottom section scrollable on small screens</name>
  <files>src/screens/RecordScreen.tsx</files>
  <action>
Wrap the content inside the `bottomSection` Animated.View with a ScrollView to ensure the Discard button is always reachable on small screens.

Changes to make:

1. Add `ScrollView` to the import list from `react-native`.

2. Wrap the three child Views inside the `bottomSection` Animated.View (playbackContainer, statusContainer, actionContainer) in a single `ScrollView` component.

3. The Animated.View currently has `style={styles.bottomSection}` with `flex: 1`. Keep this unchanged so it still fills remaining space. The ScrollView inside it gets `style={{ flex: 1 }}` and `contentContainerStyle={{ alignItems: 'center', paddingTop: 20 }}` to fill and center content.

4. Move the `paddingTop: 20` from `styles.bottomSection` into the ScrollView's `contentContainerStyle` since the Animated.View is just a flex container now.

5. Update the `styles.bottomSection` StyleSheet entry: remove `paddingTop: 20`, keep `flex: 1` and `alignItems: 'center'`.

6. Add a new `styles.scrollContent` entry:
   ```
   scrollContent: {
     alignItems: 'center',
     paddingTop: 20,
     paddingBottom: 20,
   },
   ```

7. The ScrollView must have `showsVerticalScrollIndicator={false}` and `bounces={false}` to keep the visual clean. Use `contentContainerStyle={styles.scrollContent}`.

The resulting JSX structure for bottomSection becomes:

```
<Animated.View style={[styles.bottomSection, { opacity: fadeIn, transform: [{ translateY: slideUp }] }]}>
  <ScrollView style={{ flex: 1 }} contentContainerStyle={styles.scrollContent} showsVerticalScrollIndicator={false} bounces={false}>
    {/* playbackContainer */}
    {/* statusContainer */}
    {/* actionContainer */}
  </ScrollView>
</Animated.View>
```

Do NOT change any logic, state, handlers, or functionality. Only add ScrollView wrapping and adjust the style for bottomSection.
  </action>
  <verify>
    <automated>cd /Users/thetnainglin/ForMyImprovement/kokoye/team-09-app && npx tsc --noEmit 2>&1 | grep -i "RecordScreen" || echo "NO_TSC_ERRORS_IN_RECORDSCREEN"</automated>
  </verify>
  <done>
    - ScrollView wraps playbackContainer, statusContainer, and actionContainer inside bottomSection
    - styles.bottomSection has no paddingTop (moved to scrollContent)
    - New styles.scrollContent exists with paddingTop: 20 and paddingBottom: 20
    - ScrollView shows no scroll indicator and does not bounce
    - TypeScript compiles without errors in RecordScreen.tsx
    - Discard button is reachable by scrolling on small screens
  </done>
</task>

</tasks>

<verification>
1. TypeScript: `npx tsc --noEmit` passes with no errors in RecordScreen.tsx
2. Visual: On small screen (or emulator with reduced height), after recording finishes, the Discard button should be visible or reachable by scrolling down within the bottom section
3. Normal screens: Layout should look identical to before -- no visual regression
</verification>

<success_criteria>
- ScrollView wraps the bottomSection children in RecordScreen.tsx
- Bottom section scrolls on small screens, making Discard button reachable
- No layout regression on normal-size screens
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/quick/260805-i03-fix-recordscreen-discard-button-not-visi/260805-i03-SUMMARY.md` when done
</output>
